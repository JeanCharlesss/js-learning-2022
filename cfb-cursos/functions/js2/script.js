// criando a função com dois parametros
function soma(n1, n2){ 
    var res = n1+n2
    document.write(res+'<br>')
}

soma(12,4) // fazendo as chamadas pra função com os valores de n1 e n2
soma(43,2)
soma(10,5)