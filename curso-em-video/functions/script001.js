// funções
function parimp(n){
    if (n%2 == 0 ){
        return 'Par!'
    }else {
        return 'Impar!'
    }
}
let resultado = parimp(5) // atribuindo na variável
console.log(resultado) // e então apresentando

console.log(parimp(89)) // apresentando dentro do console