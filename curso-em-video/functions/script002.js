// chamando a função
function soma(n1=0, n2=0){ // caso o n1 ou n2 não for apresentado, o retorno não será numero
    return n1 + n2 // colocamos valores pré definido no n1, n2.. n1=0
}

// atribuindo o valor 
console.log(soma(2, 5))
